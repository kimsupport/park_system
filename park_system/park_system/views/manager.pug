doctype html 
html 
  head 
    link(rel='stylesheet', href='/stylesheets/styleManager.css')
    link(rel='stylesheet', href='/stylesheets/query.css')
  body
  -if(!(session.manager_id)) {
    p 관리자만 접근할 수 있습니다.
  -} else {
    .whole 
    .header 
      h1 K- 주차장
      .button_cover 
        p #{session.manager_id}님! 반갑습니다.
        button.main 메인 페이지
        button.logout 로그아웃
    .body
      .default      
        p.main_text 기본 설정
        p  
          br
          | 현재 가격 : #{unit_fee} | 현재 주차 공간 : #{park_space} 
          br
          | 현재 단위 시간 : #{unit_time} | 잔고 : #{balance}
        form(action="update")
          input(type="number", name='park_space', placeholder='주차공간: 미제출시 300자리')
          input(type="number", name='unit_time', placeholder='단위시간: 미제출시 30분') 
          input(type="number", name='unit_fee', placeholder='기본요금: 미제출시 5000원')
          input(type="number", name='balance', placeholder='잔고설정: 미제출시 0원')
          button(type="submit") 등록
      .coupon 
        p.main_text 쿠폰 등록
        form(action="cou_reg") 
          input(type="text", name="coupon_code", placeholder='쿠폰 코드 입력')
          input(type="number", name="coupon_price", placeholder='할인금액:미제출시 5000원')
          button(type="submit") 등록
      .car_reg
        p.main_text 차량 등록
        form(action="car_reg")
          input(type='text', name="car_number", placeholder='차량 번호 입력')
          input(type='date', name='routine_term')
          button(type="submit") 등록
      .look_up_cou 
        p.lookup_cou.main_text 쿠폰 조회  
      .look_up_car    
        p.lookup_car.main_text 전체 차량 조회
      .look_up_log  
        p.lookup_log.main_text 주차장 이용 기록 조회
        
    footer 90 MINIPROJECT KIMDAEYOUNG
  -}
    
  script(src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous")
  script(src="/javascripts/scriptManager.js")